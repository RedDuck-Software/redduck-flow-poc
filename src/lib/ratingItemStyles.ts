import { RoundedStar } from '@smastrom/react-rating';

export const ratingItemStyles = {
  itemShapes: RoundedStar,
  activeFillColor: 'hsl(var(--card-foreground))',
  activeStrokeColor: 'hsl(var(--card-foreground))',
  itemStrokeWidth: 2,
  inactiveFillColor: 'hsl(var(--card))',
  inactiveStrokeColor: 'hsl(var(--border))',
};
